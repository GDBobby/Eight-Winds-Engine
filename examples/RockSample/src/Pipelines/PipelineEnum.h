#pragma once

#include <EWEngine/Graphics/Pipeline.h>

namespace EWE {
	namespace Pipe {
		enum Pipe_App_Enum : PipelineID {
			background = ENGINE_MAX_COUNT,
			grass2,
			billboard,
			Grid2d,
		};
	}
}